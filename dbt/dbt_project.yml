name: pipeline_recon_ads
version: "1.0"
config-version: 2

profile: pipeline_recon_ads

model-paths: ["models"]
macro-paths: ["macros"]

models:
  pipeline_recon_ads:
    stg:
      +materialized: ephemeral
    int:
      +materialized: ephemeral
    mart:
      +materialized: table
      +tags: ["mart"]

vars:
  company: "{{ env_var('COMPANY') }}"
  department: "{{ env_var('DEPARTMENT') }}"
  account: "{{ env_var('ACCOUNT') }}"
  